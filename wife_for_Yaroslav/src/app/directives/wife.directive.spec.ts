import { WifeDirective } from './wife.directive';

describe('WifeDirective', () => {
  it('should create an instance', () => {
    const directive = new WifeDirective();
    expect(directive).toBeTruthy();
  });
});
